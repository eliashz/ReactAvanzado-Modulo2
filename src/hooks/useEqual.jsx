import { useEffect, useState } from "react"

const useEqual = (shape, size, color) => {

    
/*     useEffect(() => {   
        setSizeColor({ ...sizeColor, [shape]: { size, color }})
        if (sizeColor.Triangle.size === sizeColor.Circle.size && sizeColor.Circle.size === sizeColor.Square.size) {
            alert("Todas las figuras tienen el mismo tamaño.")
        }
        if (sizeColor.Triangle.color === sizeColor.Circle.color && sizeColor.Circle.color=== sizeColor.Square.color) {
            alert("Todas las figuras tienen el mismo color.")
        } 
    }, [color, size]) */
}

export default useEqual;